resources:
  jobs:
    data_generator:
      name: iot-data-generator
      queue:
        enabled: true
      trigger:
        periodic:
          interval: 1
          unit: HOURS
      tasks:
        - task_key: generate_data
          notebook_task:
            notebook_path: ../src/iot_etl/ingestion/generate_steel_plant_data.ipynb