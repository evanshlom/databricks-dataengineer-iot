resources:
  jobs:
    sensor_ingest:
      name: iot-sensor-ingest
      queue:
        enabled: true
      trigger:
        periodic:
          interval: 1
          unit: HOURS
      tasks:
        - task_key: fetch_sensor_data
          notebook_task:
            notebook_path: ../src/iot_etl/ingestion/fetch_sensor_data.ipynb