# databricks.yml
# Steel plant thermo-hydraulics predictive maintenance pipeline.

bundle:
  name: iot-hello-world

variables:
  catalog:
    description: "Unity Catalog catalog for pipeline output"
    default: catalog1
  schema:
    description: "Unity Catalog schema for pipeline output"
    default: iot_hello_world

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-812d2e08-3d6c.cloud.databricks.com